package dev.thezexquex.yasmpp.core.hooks.externalhooks;

import dev.thezexquex.yasmpp.core.hooks.Hook;

public class PlaceholderApiHook extends Hook {
    @Override
    public String getIdentifier() {
        return "PlaceholderAPI";
    }
}
